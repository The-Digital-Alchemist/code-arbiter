{
  "task_id": "fix_event_counter",
  "description": "The following function has two bugs: it counts events before the window start (wrong j loop bounds) and is O(n^2). Fix both. Use a sliding window for O(n) time. The function should return the maximum number of events falling within any window of exactly `window` seconds, inclusive on both ends. Name the fixed function count_events_in_window.",
  "language": "python",
  "test_file": "test_fix_event_counter.py",
  "timeout": 10,
  "docker_image": "eval-engine-python:latest",
  "task_type": "bugfix",
  "solution_template": "def count_events_in_window(events: list[int], window: int) -> int:\n    max_count = 0\n    for i in range(len(events)):\n        count = 0\n        for j in range(len(events)):\n            if events[j] - events[i] <= window:\n                count += 1\n        max_count = max(max_count, count)\n    return max_count"
}
